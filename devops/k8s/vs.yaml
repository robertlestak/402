---
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: hpay
  namespace: humun-client-svcs
spec:
  hosts:
  - "api.402.sh"
  gateways:
  - istio-system/ingressgateway
  http:
  - match:
    - uri:
        prefix: "/"
    route:
    - destination:
        host: hpay
